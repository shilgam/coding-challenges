# python


## Prerequisites

Docker and docker-compose installed

## Usage


1. cd into directory

1. Build the Docker image:

        $ docker-compose build

1. Run the test suite:

        $ docker-compose up
